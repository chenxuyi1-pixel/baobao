# 抱抱她 v1.4｜8 月 27 日执行状态

## 已完成（无需真实接口）

- P0-1 Mock 对话接入：采用审核后的结构化对话库，不按轮次硬塞台词。
- P0-2 首页结构：顶部“就陪我说说”兜底入口，下方英姐、沈有才、小多米三张角色卡，名称已统一。
- P0-3 Token 控费：历史压缩为最近 6 条消息，`max_tokens` 调整为 200，四个角色 System Prompt 均少于 200 字。
- TTS 链路骨架：服务端新增 `/api/tts`，支持豆包语音 V3 SSE 新版 API Key 或旧版 AppID/Access Token 鉴权；失败自动回退浏览器系统语音。
- 自动回归：48 条对话、角色、安全分流和 Prompt 长度检查通过。

## 今天接下来的正确顺序

1. 用户确认账户可调用后，只验证一次真实文本接口；成功再继续，失败立即回 Demo，避免重复消耗。
2. 核对豆包语音服务所需的 AppID、Access Token、Resource ID 和已开通音色权限。
3. 在 `.env.local` 配置语音凭证后，验证 `/api/tts`，失败自动使用系统语音。
4. 确认 ASR 的真实官方地址后，再实现服务端 `/api/asr`，不把凭证放到前端。
5. 完成“录音→ASR→文本回复→TTS→播放”闭环。
6. 使用 HTTPS 测试地址进行 iPhone 麦克风与 Safari 兼容测试。

## 当前阻塞与风险

- 文本真实接口：需要账户余额/套餐可用后才能验证；代码已可切换 live。
- TTS 真实接口：代码已接好，但当前 `.env.local` 未配置独立语音凭证，因此状态仍为 `ttsReady=false`。
- 附件中的 Agent Plan TTS、ASR 路径尚未在官方 Agent Plan API 参考中确认，TTS 已改接官方豆包语音 V3 SSE 路径。
- 官方豆包语音公共云接口通常还需要独立语音应用凭证和资源权限，不能先假设只用 Ark Key 即可调用。
- iPhone 通过局域网 HTTP 可以打开页面，但麦克风需要 HTTPS。

## 明确不做

- 不在前端写入任何真实密钥。
- 不原样使用“虚构英姐真实育儿经历、沈有才攻击具体人物、小多米制造情绪依赖”的内容。
- 未核对官方接口与服务权限前，不反复调用收费语音接口。
